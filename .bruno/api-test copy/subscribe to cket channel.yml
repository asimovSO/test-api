info:
  name: subscribe to cket channel
  type: websocket
  seq: 14

websocket:
  url: "{{socket}}/app/reverbkey?protocol=7&client=js&version=8.4.0&flash=false"
  auth:
    type: bearer
    token: "{{token2}}"

settings:
  timeout: 0
  keepAliveInterval: 0

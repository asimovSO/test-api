info:
  name: get all user conversations
  type: http
  seq: 8

http:
  method: GET
  url: "{{base}}/conversations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

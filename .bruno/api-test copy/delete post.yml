info:
  name: delete post
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base}}/posts/2"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

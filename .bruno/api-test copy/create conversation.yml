info:
  name: create conversation
  type: http
  seq: 7

http:
  method: POST
  url: "{{base}}/conversations/4"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

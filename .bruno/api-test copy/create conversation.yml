info:
  name: create conversation
  type: http
  seq: 7

http:
  method: POST
  url: "{{base}}/conversations/group"
  body:
    type: json
    data: |-
      {
        "name": "test name group",
        "user_ids": [1, 3]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

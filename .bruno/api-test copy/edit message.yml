info:
  name: edit message
  type: http
  seq: 12

http:
  method: PUT
  url: "{{base}}/messages/19"
  body:
    type: json
    data: |-
      {
        "body": "edited message"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

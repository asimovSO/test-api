info:
  name: mark as read messages in chat
  type: http
  seq: 12

http:
  method: PUT
  url: "{{base}}/conversastions/2/mark-as-read"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: get messages in conversation
  type: http
  seq: 10

http:
  method: GET
  url: "{{base}}/conversations/1/messages"
  body:
    type: json
    data: |-
      {
        "body": "hello 2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

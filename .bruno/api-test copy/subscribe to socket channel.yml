info:
  name: subscribe to socket channel
  type: http
  seq: 13

http:
  method: POST
  url: http://127.0.0.1:8000/broadcasting/auth
  body:
    type: form-urlencoded
    data:
      - name: socket_id
        value: "20865269.890053281"
      - name: channel_name
        value: conversation.1
  auth:
    type: bearer
    token: "{{token2}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

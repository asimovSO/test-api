info:
  name: get all posts
  type: http
  seq: 3

http:
  method: GET
  url: "{{base}}/posts"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: send message in conversation
  type: http
  seq: 9

http:
  method: POST
  url: "{{base}}/conversations/2/messages"
  body:
    type: json
    data: |-
      {
        "body": "hello 3"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: create group chat
  type: http
  seq: 4

http:
  method: POST
  url: "{{base}}/conversations/group"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: soc
  type: websocket
  seq: 14

websocket:
  url: "{{socket}}/app/reverbkey?protocol=7&client=js&version=8.4.0&flash=false"
  message:
    type: json
    data: |-
      {
        "event": "pusher:subscribe",
        "data": {
          "channel": "conversation.1",
          "auth": "reverbkey:cfc2536400a8306891ba9fb873252e8cbce89070373f0f4ea0544f0e41f8a332"
        }
      }
  auth:
    type: bearer
    token: "{{token2}}"

settings:
  timeout: 0
  keepAliveInterval: 0
